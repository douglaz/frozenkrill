{"id":"frozenkrill-1si","title":"Test-only path removal + CLI preflight dedupe + stale comment trim","description":"Three smaller simplifications from the second codex review, grouped\nbecause they're all P3 cleanups and touch CLI / wallet_description\nboundaries that the previous bead deliberately leaves alone.\n\n## Findings to fix\n\n### 1. Remove test-only `to_singlesig` rebuild path (~40-50 lines)\n\n`SinglesigShareMetadataV0::to_singlesig()` (around\n`wallet_description.rs:1391-1397`) is `#[cfg(test)]` and reimplements\nmost of `VssRecovery::rebuild_singlesig()`:\n- Empty-passphrase normalization (`Some(\"\")` → `None`)\n- `network` / `script_type` parsing\n- No-passphrase witness generation\n- xpub mismatch check\n\nEvery caller is a test in `secret_sharing.rs::tests`. Route those\ntests through `VssRecovery::rebuild_singlesig` (or a tiny shared\nhelper if one rebuild path argues for it).\n\nThe trade-off: tests get slightly noisier setup (build a `VssRecovery`\nfirst), but only one recovery policy lives in the codebase.\n\n### 2. Deduplicate split-secret preflight checks (~20-30 lines)\n\n`commands::split_secret::split_singlesig_wallet()` runs M-of-N and\noutput-dir validation (`split_secret.rs:70-76`) that\n`Commands::SplitSecret` in `main.rs` already runs before any prompt.\nNow that `split_singlesig_wallet()` is `pub(crate)` and only called\nfrom that one CLI path, the second copy is defensive scaffolding.\n\nEither:\n- Drop the duplicate `ensure!`s entirely (rely on the CLI-side\n  preflight), OR\n- Extract a `preflight_split_secret(...)` helper both can call (if\n  the CLI ever needs to skip a check, this gives a single knob).\n\nPrefer the simpler \"drop the duplicate\" option unless extracting\nthe helper provides immediate clarity.\n\n### 3. Trim stale post-combine comment (~15 lines)\n\n`commands/combine_secret.rs:146-149` carries a multi-paragraph comment\ndescribing a \"cross-check / warn and still print the seed\" local flow,\nbut the code below no longer does any of that — authentication now\nlives entirely inside `combine_vss_wallets()`. Collapse the comment\nto a one-liner pointing at the library function.\n\n## Acceptance criteria\n\n- `nix develop --command cargo build --workspace` clean\n- `nix develop --command cargo test --workspace` green —\n  every existing test passes (some tests' setup may shift from\n  `to_singlesig` to `rebuild_singlesig`)\n- `nix develop --command cargo clippy --workspace --all-targets -- -D warnings` clean\n- `nix develop --command cargo fmt --check` clean\n- `nix build` passes\n- **Net line reduction across `wallet_description.rs`,\n  `split_secret.rs`, and `combine_secret.rs` of at least 60 lines\n  combined** (conservative lower bound).\n- The recovery-authentication path in `combine_vss_wallets` is the\n  one and only `(seed → no-passphrase wallet → xpub match)` check\n  in the codebase.\n\n## Out of scope\n\n- Anything in the core split/combine path of `secret_sharing.rs`\n  beyond what's strictly needed to keep tests passing after the\n  `to_singlesig` removal.\n- Functional behavior changes — these are pure cleanups.\n\nSource: codex simplification review #2 of feature/pedersen-secret-sharing.","status":"open","priority":3,"issue_type":"task","created_at":"2026-05-02T23:34:20.317146520Z","created_by":"assistant","updated_at":"2026-05-02T23:34:20.317146520Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["codex-review","refactor","simplification"]}
{"id":"frozenkrill-gxv","title":"Upgrade vsss-rs from 4.3.8 to 5.4.0","description":"Upgrade the `vsss-rs` dependency from 4.3.8 to 5.4.0 (latest published).\n\n## Background\n\n`frozenkrill-core` uses `vsss-rs` for the Pedersen Verifiable Secret Sharing\ncore that powers the new `split-secret`/`combine-secret` commands. The 5.x\nline is a breaking API rewrite — the upgrade isn't just a Cargo.toml bump.\n\n## API changes 4.x → 5.x\n\n| Surface | v4 (current) | v5 (target) |\n|---|---|---|\n| Share data type | `Vec<u8>` (raw bytes; first byte = share identifier) | `DefaultShare<IdentifierPrimeField<WrappedScalar>, IdentifierPrimeField<WrappedScalar>>` (typed scalar + typed identifier) |\n| `StdPedersenResult` type params | `<G, Id, Share>` (3 params) | `<S, V>` (2 params) |\n| `pedersen::split_secret` secret arg | by value | by reference (`&S::Value`) |\n| `combine_shares` | free function in `vsss_rs::` | now `result.combine()` method on the result |\n| Share identifier extraction | `share_bytes[0]` | typed `IdentifierPrimeField<WrappedScalar>` field on the share |\n| Verifier set | `vsss_rs::PedersenVerifierSet` trait | `pedersen::Pedersen` re-export + restructured trait surface |\n\n## Files to change\n\nPrimary site (only one file in src actually imports vsss-rs):\n- `frozenkrill-core/src/secret_sharing.rs` — touches `vsss_rs::curve25519::{WrappedRistretto, WrappedScalar}`,\n  `vsss_rs::pedersen::{StdPedersenResult, split_secret}`, and\n  `vsss_rs::{PedersenResult, PedersenVerifierSet, combine_shares}`. Specifically:\n  - `split_one_scalar` (the StdPedersenResult call site)\n  - `combine_one_half` (the `combine_shares` call site)\n  - `verified_unique_share_pairs` (verifier-vec / `verify_share_and_blinder` calls)\n  - `parse_ristretto_points` (verifier hex parser)\n  - All of the candidate-cross-product enumeration in `combine_vss_wallets`\n    that depends on `share_bytes[0]` being the identifier byte\n- `frozenkrill-core/Cargo.toml` — `vsss-rs = { version = \"5\", features = [\"curve25519\"] }`\n\n## On-disk format implications\n\nOur share files persist `share_data` and `blinder_share_data` as\nhex-encoded bytes, currently the raw byte format vsss-rs 4.x emits. v5\nserializes typed scalars differently. This is a one-time on-disk format\nbreak — acceptable because the secret-sharing feature has not been\nreleased yet (it's still the open PR #33 against `master`, no users have\ncommitted share files yet).\n\nThe `verification_data` (Pedersen verifier set as comma-separated hex of\ncompressed Ristretto points) MAY be unchanged since Ristretto compression\nis a well-established encoding — verify this during the migration rather\nthan assuming.\n\n## Identifier extraction — careful migration\n\nSeveral invariants in the codex-hardened combine path key off\n`secret_share[0]` being the share identifier byte:\n- `verified_unique_share_pairs` dedupe (lines ~556-583)\n- The candidate-cross-product enumeration in `combine_vss_wallets` (lines ~882-940)\n- HH-2 / II-2 metadata-vote dedupe via 'cryptographic share identifier (first byte of share_data)'\n\nThese all need to be rewritten to use v5's typed identifier instead.\nThe semantics should be preserved (dedupe by share identifier, refuse\nties), but the byte-level access goes away.\n\n## Test surface\n\nThe `secret_sharing` test module has 30+ tests covering:\n- Round-trip correctness (12-word and 24-word)\n- Tampered-share rejection (Pedersen-verification negative cases)\n- Per-share verifier corruption tolerance\n- Mixed lo/hi verifier corruption (cross-product candidate enumeration)\n- Metadata-tamper guards (xpub mismatch, downgrade attacks)\n- Duress-mode aggregation\n- All of these must continue to pass. Several that currently construct\n  `Share` values directly (or compare share_data hex against expected\n  byte patterns) may need adaptation to the new types.\n\n## Acceptance criteria\n\n- `vsss-rs = \"5\"` in `frozenkrill-core/Cargo.toml`\n- `cargo build` clean (workspace)\n- `cargo test --workspace` green — all existing secret_sharing tests pass\n- `cargo clippy --all-targets -- --deny warnings` clean\n- `cargo fmt --check` clean\n- The cryptographic semantics are preserved:\n  - Same curve (Curve25519/Ristretto via `vsss_rs::curve25519::WrappedRistretto`/`WrappedScalar`)\n  - Same Pedersen blind verification (`g^a · h^b` commitments, `verify_share_and_blinder`-equivalent check)\n  - Same threshold-secret-sharing behavior\n- The on-disk `share_data`/`blinder_share_data` hex format change is\n  documented inline in `secret_sharing.rs` (the format is now version-tied\n  to vsss-rs 5.x)\n- All codex-hardened invariants still hold (per-share dedupe by\n  cryptographic identifier, fail-closed metadata authentication,\n  candidate cross-product enumeration for split lo/hi corruption, etc.)\n\n## Out of scope\n\n- Bumping `rand`/`rand_core` to 0.9 — separate concern; vsss-rs 5.x still\n  pins `rand_core = \"0.6\"` on its main branch, so this upgrade doesn't\n  unblock the rand bump.\n- Bumping `bip39` past `>=0.6, <0.9` cap — same reason.\n- Refactoring the surrounding wallet_description / share-encryption /\n  CLI code beyond what's necessary to fit the new vsss-rs types.\n\nSource: follow-up to PR #33 cleanup; user asked for the vsss bump after\nthe codex-review-loop converged on v4.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-02T01:30:33.416005432Z","created_by":"assistant","updated_at":"2026-05-02T03:16:23.704705347Z","closed_at":"2026-05-02T03:16:23.704365503Z","close_reason":"Implemented in commit 7c6c6a6 (douglaz/frozenkrill#1, squash-merged into feature/pedersen-secret-sharing). Migrated frozenkrill-core/src/secret_sharing.rs to vsss-rs 5.4.0's typed Share/Identifier API; updated Cargo.toml/Cargo.lock; added flake.nix packages.default for the nix build gate. Validated: cargo fmt --check, cargo clippy --workspace --all-targets -- -D warnings, cargo test --workspace (80 passed), nix build. Implemented via ralph-burning iterative_minimal flow (4 rounds; amendments converged 1→1→2→0).","source_repo":".","compaction_level":0,"original_size":0,"labels":["crypto","deps","refactor"]}
{"id":"frozenkrill-hd8","title":"Simplify secret_sharing.rs (collapse Share regroup, gate checksum to tests, unify verifier check, trim comments)","description":"Codex simplification review identified four overlapping refactors all\ninside `frozenkrill-core/src/secret_sharing.rs`. Land them together\nbecause they touch the same recovery path.\n\n## Findings to fix\n\n### 1. Combine candidates directly instead of cloning `Share`s (~150-200 lines)\n`combine_vss_wallets` (lines ~1150-1154 in the pre-vsss5 layout) iterates\none verifier candidate at a time, but still clones every input wallet\ninto a synthetic `Share` so `combine_shares_to_mnemonic` can regroup\nthem by those same verifier strings. Fold that helper into a candidate-\nspecific combine over `VssJsonWalletDescriptionV0` directly. Behavior\nmust be identical; we're just dropping the cloning round-trip.\n\n### 2. Remove test-only `Share` checksum from production flow (~60-80 lines)\nThe internal `Share::checksum` field and `compute_checksum()` method\nare only exercised by tests now. Production split/combine still carries\nthe field and recomputes it for synthetic shares built during recovery.\nEither:\n- Gate `checksum` field + `compute_checksum`/`verify_checksum` behind\n  `#[cfg(test)]`, OR\n- Drop the field entirely and adapt the few tests that assert on it\n  to go through the AEAD-backed `VssJsonWalletDescriptionV0` path\n  instead.\n\nPrefer the drop-entirely option if the test surface is small enough.\n\n### 3. Single verifier-check helper across recovery paths (~40-60 lines)\nThe `verifies_lo` / `verifies_hi` closures inside `combine_vss_wallets`\n(lines ~1183-1186 pre-vsss5) repeat the hex-decode + share-pair\nverification work that `derive_metadata` re-implements a few lines\nlater. Extract `wallet_verifies_against_candidate(...)` (or similar)\nand have both call sites use it. One helper, one place to change the\nacceptance rules.\n\n### 4. Trim Pedersen/Shamir tutorial text (a few hundred lines)\n`secret_sharing.rs` opens with a tutorial-style module preface and\nrepeats 10-20 line threat-model explanations beside many individual\nbranches. Most of the long-form rationale is now duplicated in\n`docs/secret_sharing.md` (added during the codex hardening loop).\nTrim the inline comments to short intent statements (\"Why we abstain\non tied votes\"), and lean on the doc for the security narrative.\n\nDO NOT delete comments that:\n- Document a non-obvious invariant the code itself doesn't show\n  (e.g. \"the first byte of share_data IS the share identifier\" — but\n  with vsss-rs 5.x's typed identifiers, even some of those are now\n  obsolete).\n- Explain WHY a particular fail-closed branch was added (the codex-\n  loop letter-tagged findings — EE-1, FF-2, GG-1, etc. — preserved\n  the WHY, and that's load-bearing context for future readers).\n\nDO trim:\n- Multi-paragraph threat-model walkthroughs that just restate the\n  Pedersen / Shamir definitions.\n- Repeated \"this is hardened against X\" notes when the test suite\n  + docs already make X explicit.\n- Bullet-list explanations longer than the code they annotate.\n\n## Acceptance criteria\n\n- All existing tests in `frozenkrill-core/src/secret_sharing.rs::tests`\n  still pass (currently 68+ tests after vsss-rs 5.x migration).\n- `cargo build --workspace` clean\n- `cargo test --workspace` green\n- `cargo clippy --workspace --all-targets -- -D warnings` clean\n- `cargo fmt --check` clean\n- `nix build` passes\n- Net line reduction in `frozenkrill-core/src/secret_sharing.rs`\n  of at least 250 lines (the four findings combined, conservative\n  lower bound).\n- All cryptographic semantics preserved — same Pedersen verification,\n  same threshold reconstruction, same fail-closed authentication.\n\n## Out of scope (will be a separate bead)\n\n- Dropping `OriginalWalletJson` tagged enum in `wallet_description.rs`.\n- Unifying the VSS encryption envelope with the singlesig/multisig\n  encoder helpers in `lib.rs`.\n\nSource: codex simplification review of feature/pedersen-secret-sharing\nvs origin/master (post-vsss-5 upgrade).","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-02T04:50:40.383838313Z","created_by":"assistant","updated_at":"2026-05-02T05:17:48.000287653Z","closed_at":"2026-05-02T05:17:47.999824378Z","close_reason":"Implemented in commit fe7d654 (douglaz/frozenkrill#2, squash-merged). secret_sharing.rs: 3008→2662 lines (-346). All 4 codex findings landed: Share regroup collapsed, checksum dropped, verifier-check helper unified, threat-model comments trimmed. Validated: cargo fmt --check, cargo clippy --workspace --all-targets -- -D warnings, cargo test --workspace (79 passed; -1 was the test for the dropped Share::checksum field), nix build. Implemented via ralph-burning iterative_minimal flow (1 round, 0 amendments).","source_repo":".","compaction_level":0,"original_size":0,"labels":["codex-review","refactor","simplification"]}
{"id":"frozenkrill-ugd","title":"Collapse internal Share schema + drop SuccessEntry verifier fields","description":"Two structural simplifications inside `frozenkrill-core/src/secret_sharing.rs`\nidentified by the second codex simplification review. Land together —\nboth touch the recovery path's internal data shapes.\n\n## Findings to fix\n\n### 1. Collapse the duplicate internal `Share` schema (~150-200 lines)\n\n`Share` (defined around `secret_sharing.rs:158-159`) now shadows almost\nevery field on the public `VssJsonWalletDescriptionV0`:\n- version, share_index, threshold, total_shares, mnemonic_length\n- share_data, blinder_share_data, verification_data\n- share_data_hi, blinder_share_data_hi, verification_data_hi\n- created_at, scheme\n\nThe rest of the file pays for that second schema twice:\n- `split_singlesig_wallet()` builds `Share` values then immediately\n  clones every field into the public `VssJsonWalletDescriptionV0`.\n- `combine_shares_to_mnemonic()` (and the candidate-cross-product\n  enumeration in `combine_vss_wallets`) clones inputs back into\n  synthetic `Share` values just to call internal helpers.\n\nDrop the internal `Share` struct. Every code path that uses it\nshould consume `VssJsonWalletDescriptionV0` directly, OR a smaller\nshared payload type both structs embed (if a clean factoring exists).\n\nThe trade-off: raw-mnemonic tests (currently in `secret_sharing.rs`'s\ntest module) become slightly more end-to-end because they go through\n`VssJsonWalletDescriptionV0` instead of the lower-level `Share`. But\nsplit/combine rules then live in exactly one place.\n\n### 2. Drop redundant verifier fields from `SuccessEntry` (~40-60 lines)\n\n`SuccessEntry` (around `secret_sharing.rs:900-906`) currently looks\nroughly like:\n\n```rust\ntype SuccessEntry<'a> = (\n    SecretBox<Mnemonic>,\n    Vec<&'a VssJsonWalletDescriptionV0>,  // verified_inputs\n    String,                                // lo verifier hex (redundant)\n    Option<String>,                        // hi verifier hex (redundant)\n);\n```\n\nThe `verified_inputs` already encode the candidate verifier — every\ninput in there carries the same `verification_data` / `verification_data_hi`\nthat we actually verified against. `derive_metadata()` then re-parses\nthose redundant strings just to rerun the same per-share filter that\nalready produced `verified_inputs`.\n\nChange `derive_metadata()` to consume the verified group directly\n(reading the verifier off any element, since they all match by\nconstruction). Drop the two trailing tuple fields and the\n\"why we carry these\" comment.\n\nThe trade-off: `SuccessEntry` stops being self-sufficient for\nre-verifying its own inputs later. Nothing currently uses it that\nway, so the loss is only theoretical.\n\n## Implementation order\n\n1. **Finding 2 first** (drop verifier tuple fields). Smaller, more\n   localized — sets up `derive_metadata` to read verifiers off\n   `verified_inputs`, which is also the shape the bigger refactor\n   in finding 1 will rely on.\n2. **Finding 1 second** (collapse `Share`). Bigger structural\n   change — easier to reason about once `SuccessEntry` is simpler.\n\n## Cryptographic semantics — DO NOT change\n\n- Pedersen blind-commitment verification (`g^a · h^b` per share).\n- Threshold reconstruction (M-of-N Shamir).\n- Fail-closed authentication in `combine_vss_wallets`.\n- Candidate cross-product enumeration for split lo/hi corruption.\n- Strict-majority `is_duress` aggregation; abstain on tie.\n- Per-share verifier-corruption tolerance (verify against the\n  winning verifier, not the share's own copy).\n\n## Acceptance criteria\n\n- All existing tests in `frozenkrill-core/src/secret_sharing.rs::tests`\n  still pass (currently 67 tests; some may be rewritten end-to-end\n  through `VssJsonWalletDescriptionV0` if `Share` goes away).\n- `nix develop --command cargo build --workspace` clean\n- `nix develop --command cargo test --workspace` green\n- `nix develop --command cargo clippy --workspace --all-targets -- -D warnings` clean\n- `nix develop --command cargo fmt --check` clean\n- `nix build` passes\n- **Net line reduction in `frozenkrill-core/src/secret_sharing.rs`\n  of at least 180 lines** (conservative lower bound for the two\n  findings combined).\n- All cryptographic semantics preserved (see \"DO NOT change\" above).\n\n## Out of scope (separate bead)\n\n- Removing the test-only `to_singlesig` path in `wallet_description.rs`.\n- Deduplicating split-secret preflight in `split_secret.rs`.\n- Trimming the stale post-combine comment in `combine_secret.rs`.\n\nSource: codex simplification review #2 of feature/pedersen-secret-sharing.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-02T23:34:20.218725773Z","created_by":"assistant","updated_at":"2026-05-03T00:55:10.390036442Z","closed_at":"2026-05-03T00:55:10.389556557Z","close_reason":"Implemented in commit 2f98a7e (douglaz/frozenkrill#4, squash-merged). secret_sharing.rs: 2622->2435 lines (-187). Both findings landed: collapsed internal Share schema; dropped redundant SuccessEntry verifier fields. Validated: cargo fmt --check, cargo clippy --workspace --all-targets -- -D warnings, cargo test --workspace (81 passed; +2 from ralph's test adaptation), nix build. Implemented via ralph-burning iterative_minimal flow (2 rounds; round 1 had 4 amendments going to arbiter which admitted 1, round 2 reached 0/0 convergence).","source_repo":".","compaction_level":0,"original_size":0,"labels":["codex-review","refactor","simplification"]}
{"id":"frozenkrill-x2z","title":"Drop OriginalWalletJson enum + unify VSS encryption envelope","description":"Two cross-file API simplifications from the codex simplification review.\nThey touch `wallet_description.rs` and `lib.rs` (and propagate through\n`secret_sharing.rs` callers). Land together as a single follow-up\nbead after the secret_sharing.rs internal cleanup lands.\n\n## Findings to fix\n\n### 1. Store singlesig metadata directly in VSS shares (~70-100 lines)\n`VssJsonWalletDescriptionV0::original_wallet` is currently typed\n`OriginalWalletJson` (a tagged enum: `Singlesig(SinglesigPublicMetadataV0)`\n| `Multisig(...)`). But every VSS share in this branch is created by\n`split_singlesig_wallet`, and recovery only ever consumes\n`SinglesigPublicMetadataV0`. The enum is speculative generality.\n\nChange the field to `original_wallet: SinglesigPublicMetadataV0`\ndirectly. Removes:\n- The `OriginalWalletJson` enum definition.\n- `is_singlesig` / `is_multisig` helpers.\n- Defensive `Multisig` match arms in `to_singlesig` / `derive_metadata`\n  (and a couple of similar sites — search the codebase).\n\nIf/when a real multisig share format ever needs to be added later,\nre-introduce the enum at that point; do not pre-design for it now.\n\n### 2. Reuse standard wallet encryption envelope for VSS (~50-70 lines)\n`generate_encrypted_encoded_vss_wallet` in `frozenkrill-core/src/lib.rs`\n(lines ~397-405) is effectively a third copy of the same envelope:\nserialize → compress → encrypt → header → pad → serialize. The\nsinglesig and multisig `generate_encrypted_encoded_*` functions\nalready do this same dance.\n\nExtract a helper (e.g. `encrypt_with_standard_envelope<T: Serialize>(...)`)\nand have all three call sites use it. Reduces drift when the encrypted\nheader / padding rules change later.\n\nThe trade-off is one generic helper signature, which is still simpler\nthan maintaining three near-identical crypto wrappers.\n\n## Acceptance criteria\n\n- `cargo build --workspace` clean\n- `cargo test --workspace` green — every existing test passes,\n  including all share round-trip tests and the encryption envelope\n  tests\n- `cargo clippy --workspace --all-targets -- -D warnings` clean\n- `cargo fmt --check` clean\n- `nix build` passes\n- Net line reduction across `wallet_description.rs` and `lib.rs`\n  of at least 100 lines combined.\n- Cryptographic / on-disk format semantics unchanged: encryption\n  envelope structure, header bytes, padding rules — all identical\n  before and after.\n- The on-disk JSON layout of `VssJsonWalletDescriptionV0` may simplify\n  (the `original_wallet` field's tag / variant wrapping goes away),\n  but this is acceptable because the secret-sharing feature has not\n  been released yet (still PR #33 against master).\n\n## Out of scope\n\n- Anything in `secret_sharing.rs` (handled by the previous bead).\n- Adding multisig support — the simplification is precisely about\n  not building speculative multisig infrastructure.\n\nSource: codex simplification review of feature/pedersen-secret-sharing\nvs origin/master (post-vsss-5 upgrade).","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-02T04:50:40.518438724Z","created_by":"assistant","updated_at":"2026-05-02T06:11:00.255583388Z","closed_at":"2026-05-02T06:11:00.254967208Z","close_reason":"Implemented in commit 08a257e (douglaz/frozenkrill#3, squash-merged). Dropped OriginalWalletJson tagged enum (-86 lines in wallet_description.rs); unified VSS encryption envelope (-19 lines in lib.rs). Combined -105 lines (acceptance >=100). Type-driven knock-on edits in secret_sharing.rs (~92 lines) + docs refresh. Validated: cargo fmt --check, cargo clippy --workspace --all-targets -- -D warnings, cargo test --workspace (79 passed), nix build. Implemented via ralph-burning iterative_minimal flow (3 rounds; amendments converged 2->1->0).","source_repo":".","compaction_level":0,"original_size":0,"labels":["codex-review","refactor","simplification"]}
